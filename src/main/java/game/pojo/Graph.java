package game.pojo;

public class Graph {
    // I don't think we need this. Because all the details about neighbours of a country will be stored in the Country object itself.
    // And the countries controlled by a player will be stored in Player object.
    // We should be able to perform all actions by using those objects.
}
